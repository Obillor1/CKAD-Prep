1. Create a YAML manifest for a CronJob named hello that runs the date command in a busybox container named hello every minute, with a timeout of 22 seconds.
  - Here you use the Active DeadlineSecs for the timeout/completions
    kubectl create cronjob my-job --image=busybox --schedule="*/1 * * * *" --dry-run=client -o yaml > cron.yaml -- date
    Edit yaml file to have the dealinesec 

    activeDeadlineSeconds defines how long the Job is allowed to run before Kubernetes terminates it.
   
“Non-parallel job” = only one Pod running at a time
parralelism =1
Or from KK parralelism wasn't defined
